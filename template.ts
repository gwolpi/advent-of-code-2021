export const p1 = (input: string): number => {
  const lines = input.split('\n').map(Number);
  return lines.length;
}

export const p2 = (input: string): number => {
  const lines = input.split('\n').map(Number);
  return lines.length;
}